; @react-spectrum/s2 declares a React 19-rc peer range, but it runs correctly on
; the React 18.3 this app is pinned to (Adobe's own Vite example ships React 18).
; legacy-peer-deps lets npm install S2 without erroring on that peer mismatch.
legacy-peer-deps=true
